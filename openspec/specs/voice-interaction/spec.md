# voice-interaction Specification

## Purpose

Defines the provider-independent voice-turn contract and the runtime behavior that accepts a validated turn and returns a deterministic placeholder reply, without treating a voice vendor as the agent or session system of record.

## Requirements

### Requirement: Voice turns use an internal provider-independent contract

The runtime MUST represent an inbound voice interaction as an internal contract that does not use a voice-vendor type. The contract MUST include, when available: a session identifier, an interaction or request identifier, the received input text, a timestamp, an event type, and opaque correlation metadata. Provider call identifiers MUST be stored only as opaque external channel identifiers, not as domain identity.

#### Scenario: Valid turn carries correlation fields

- **WHEN** the runtime receives a validated inbound voice turn with session, interaction, and request identifiers
- **THEN** those identifiers are present on the internal contract and are usable for logging and debugging

#### Scenario: Contract has no vendor types

- **WHEN** a reviewer inspects domain and application representations of a voice turn
- **THEN** those representations do not import or embed a voice-vendor SDK type or vendor-native payload schema

### Requirement: Media identity is not business state

The runtime MUST keep channel or media identity distinct from product business state. This change MUST NOT introduce conversational memory or persist Session or ConversationTurn tables. A successful agent turn MUST NOT advance a product business workflow.

#### Scenario: Successful placeholder does not create business state

- **WHEN** the runtime handles a valid voice turn
- **THEN** it returns a structured agent reply without writing a product Session aggregate or changing a business workflow status

#### Scenario: External channel id stays opaque

- **WHEN** an inbound turn includes a provider call or conversation identifier
- **THEN** the runtime treats it as an opaque external channel identifier and does not use it as the internal session primary identity unless no internal session identifier was supplied

### Requirement: Runtime returns a runtime-owned agent reply

Given a valid internal voice turn, the application runtime MUST produce a structured reply owned by the runtime agent-turn path. The reply text MUST come from a schema-valid agent `reply` decision after any authorized tool hop. The reply MUST NOT be generated by the voice vendor or by adapter-local business rules. Locale for the reply MUST come from the turn or configuration, not from a hard-coded domain language rule. The default configured locale remains Spanish (`es`). Tests that use a mocked LLM MUST remain deterministic for the same fixture and locale.

#### Scenario: Valid turn yields agent reply

- **WHEN** the runtime handles a valid voice turn and the mocked or configured LLM returns a schema-valid reply
- **THEN** it returns a structured reply whose text is the agent `replyText` for the configured locale and the LLM port is used

#### Scenario: Same mocked input is deterministic

- **WHEN** the runtime handles two equivalent valid voice turns with the same configured locale and the same mocked LLM fixture
- **THEN** the structured reply content is the same

### Requirement: Agent failures map to typed voice-safe errors

When the agent-turn path fails, the voice-turn result MUST remain a typed voice error with a stable code and a safe message. Agent `llm_timeout` and `tool_timeout` MUST map to the existing voice timeout classification. Other agent failures MUST map to an existing voice-safe code or an added voice-safe code that does not leak internals. Stack traces, secrets, and raw model output MUST NOT appear on the structured voice error.

#### Scenario: Agent timeout is a voice timeout

- **WHEN** agent handling exceeds the configured voice handling timeout or the LLM time budget
- **THEN** the voice-turn result is a timeout voice error and does not leave an unbounded wait

#### Scenario: Invalid model output is voice-safe

- **WHEN** the agent-turn path fails with `invalid_output`
- **THEN** the voice-turn result is a typed error with a stable code and a safe message and omits raw model output

### Requirement: Runtime is invocable without a voice vendor

The voice-turn use case MUST be executable through the internal contract alone. Tests and local invocation MUST NOT require a voice-vendor account, SDK, or network call.

#### Scenario: In-process invocation without vendor

- **WHEN** a test supplies a valid internal voice turn to the runtime use case
- **THEN** the use case returns a structured reply or typed voice error without importing or calling a voice vendor

### Requirement: Voice errors are typed and safe

The runtime MUST classify voice-handling failures with stable codes covering at least: invalid payload, invalid session, unsupported event, timeout, configuration error, provider error, and internal runtime error. Internal exception details, stack traces, and secrets MUST NOT be placed on the structured reply returned to an adapter.

#### Scenario: Runtime failure is typed

- **WHEN** the voice-turn use case fails because of an internal error or a bounded timeout
- **THEN** the result is a typed voice error with a stable code and a safe message

#### Scenario: Timeout is bounded

- **WHEN** handling a voice turn exceeds the configured handling timeout
- **THEN** the runtime returns a timeout voice error and does not leave an unbounded wait

#### Scenario: Successful turn cancels the handling timer

- **WHEN** a voice turn completes before the handling timeout
- **THEN** the runtime cancels the timeout timer and MUST NOT emit an unhandled rejection from that timer

### Requirement: Voice turns emit correlatable secret-safe logs

Each handled voice turn MUST emit a structured log that, when the values are available, includes session identifier, request identifier, interaction identifier, the turn `occurredAt` timestamp as ISO-8601, event type, processing time, status, and the same `traceId` used on observability spans for that execution. Logs MUST NOT contain API keys, credentials, or unnecessary sensitive payload fields.

#### Scenario: Successful turn is correlatable

- **WHEN** a valid voice turn completes
- **THEN** a success log includes the available correlation identifiers, the execution `traceId`, the turn `occurredAt` timestamp, event type, processing time, and a success status

#### Scenario: Failed turn is correlatable

- **WHEN** a voice turn fails
- **THEN** a failure log includes the available correlation identifiers, the execution `traceId` when one was assigned, a stable error code, and no secret values

#### Scenario: Invalid session identifier is not logged raw

- **WHEN** the runtime rejects a turn because the session identifier is not a UUID
- **THEN** the failure log includes a stable session-invalid code and MUST NOT include the raw invalid identifier

### Requirement: Configured session owner supplies the spoken reply

When the configured voice session owner is `wom-customer-service-agent`, a valid internal voice turn MUST produce a structured reply owned by that agent through the existing `VoiceTurn` / `VoiceReply` contract. Reply text MUST come from a schema-valid WOM `reply` decision after any authorized `wom.*` tool hop. The voice handling timeout, locale default `es`, and existing agent-to-voice error mapping MUST remain unchanged.

#### Scenario: WOM usage turn maps to VoiceReply

- **WHEN** inbound handling uses the WOM session owner and the mocked LLM selects `wom.get_customer_usage` then a Spanish `reply`
- **THEN** the voice-turn result is a structured success whose text is that `replyText`

#### Scenario: Default owner regression

- **WHEN** session-owner configuration is omitted
- **THEN** a valid mocked `runtime-demo` turn still yields a structured agent reply under the existing contract

### Requirement: Frontend call state is not business session state

A demonstration UI MAY track media or call presentation state (`idle`, `connecting`, `active`, `ending`, `completed`, `error`). That presentation state MUST remain distinct from runtime business session state. Completing or ending the UI call MUST NOT create a persisted Session or ConversationTurn aggregate in this change and MUST NOT be treated as proof that the backend finished asynchronous end-of-call processing.

#### Scenario: UI completed does not imply backend persistence

- **WHEN** the demonstration UI enters `completed` after the media client reports call end
- **THEN** the runtime is not required to have persisted a Session or ConversationTurn and the UI MUST NOT claim a durable execution report exists

#### Scenario: Media identity stays opaque

- **WHEN** the media client exposes a provider call identifier
- **THEN** the demonstration UI may display call status and duration but MUST NOT use that identifier as a product customer identity

### Requirement: Voice budgets for the browser path stay on the runtime

Spoken replies and tool execution on a browser-originated call MUST continue to follow the existing runtime voice-turn contract and timeouts. The demonstration UI MUST NOT implement a second agent-turn loop or a looser tool-hop policy. Time-to-first-audio and connecting feedback are UI concerns; they MUST NOT replace runtime timeout classification.

#### Scenario: Browser call still uses runtime replies

- **WHEN** a live browser call produces a user utterance that the provider forwards as a supported inbound turn
- **THEN** the spoken reply content is still produced by the runtime use case, not invented by the demonstration UI
