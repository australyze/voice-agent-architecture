# Verification Report - UI E2E

- Date: 2026-09-06
- Change: wom-voice-demo-ui
- Agent: evaluation-engineer
- Change types: ui | voice | code
- Context: `/verify` re-run after adversarial remediations

## Commands executed

- `npm --prefix web run test`
- `npm --prefix web run typecheck`

## Results

- **20 passed**, 0 failed (6 files)
- Web typecheck: PASS
- Playwright was not added. Full-page Testing Library flows with `createFakeMediaClient` cover idle chrome, honest status, microphone-provider consent, suggested prompts (no auto-start), connecting, 8s connecting timeout → retryable error, active transcript + tool label, ending vs completed, and safe error copy
- Automated tests do not import a live Vapi constructor without a stub (`createSdk` / fake client)
- No microphone permission and no paid voice calls

## Live Vapi browser smoke (6.10 / 8.6)

- Status: **SKIP**
- Reason: no gitignored `web/.env` with `VITE_VAPI_PUBLIC_KEY` and `VITE_VAPI_ASSISTANT_ID` was present
- Not a CI failure. Still required for human Definition of Done — follow `docs/adapters/vapi-web-demo.md`

## Secret hygiene

- `web/.env.example` documents only public client variables
- Presentation files do not import `@vapi-ai/web`
- Error state tests assert no `sk-` leakage
- Frontend sources do not call `localStorage` or `sessionStorage`

## Outcome

- Status: PASS (automated). Live smoke SKIP pending operator credentials.
