# Public browser values only. Never put inbound-only secrets, private provider keys, hosted persistence admin keys, or database URLs here.
VITE_VAPI_PUBLIC_KEY=
VITE_VAPI_ASSISTANT_ID=
VITE_PUBLIC_API_BASE_URL=
# Demo-operator read secret for GET /sessions. Use the same value as the backend demo-orchestrate or inbound shared secret.
VITE_DEMO_ORCHESTRATE_SECRET=
