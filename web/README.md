# WOM voice demo UI

React + TypeScript interviewer surface. Not the AI Agent Runtime.

```bash
npm install
npm run dev
npm run test
npm run typecheck
```

Copy `.env.example` to `.env` and set the Vapi **public** key and assistant id. See `docs/adapters/vapi-web-demo.md`.
